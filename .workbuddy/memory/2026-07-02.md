# 2026-07-02

## GitHub 推送被拦截：DeepSeek API Key 泄露修复

- GitHub Secret Scanning 检测到 `worker/local-server.js` 第 4 行硬编码了 DeepSeek API Key，推送被 block。
- 修复措施：
  1. `worker/local-server.js` 改为从 `process.env.DEEPSEEK_API_KEY` 读取，启动时校验，缺失则报错退出。
  2. 新增 `.env` 文件存放真实 API Key，并加入 `.gitignore` 避免提交。
  3. 新增 `.env.example` 作为模板。
  4. 将 `worker/.wrangler/cache/` 加入 `.gitignore` 并移除已提交的 `wrangler-account.json`。
  5. 使用 `git commit --amend` 重写 `9e5bcf9` → `619bd94`，从 commit 历史中彻底清除 secret。
  6. 执行 `git push --force-with-lease origin main`，推送成功。
- 验证：HEAD 中已无 API Key，`.env` 和 `wrangler-account.json` 未进入版本库。
