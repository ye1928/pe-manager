# 2026-05-11 工作日志

## 修复归因分析周期收益问题

### 问题描述
- 总览持仓市值（¥144.72万）与归因分析本金+盈亏（¥142.06万）不一致
- 周期汇总表格显示 NaN

### 修复内容

#### 1. fmtAmt/fmtMoney 增加 undefined 防御（第3759-3762行、第3786-3789行）
- 修复 NaN/undefined 导致显示 NaN 的问题
- 新增 `v == null || isNaN(v)` 检查

#### 2. calcFundPeriod 核心修复（第3641-3729行）
- **activeCost 口径统一**：改为 `remainingShares × costNav`，与总览持仓市值口径一致
- **浮动盈亏计算修复**：使用期末单位净值 - 期初单位净值（不含分红），避免与分红收益重复计算
- **分红日期过滤修复**：分红条件改为 `d.date >= batchDate && d.date >= startDate && d.date <= endDate`
- **删除冗余变量**：移除未使用的 `cumNavAtStart`、`cumNavAtEnd`、`latestNav`
- **返回值增加字段**：`navPnlAmount`、`divPnlAmount`、`exitPnlAmount`

### 关键修复点
1. 浮动盈亏 = (期末单位净值 - 期初单位净值) × 剩余份额（不含分红）
2. 分红收益单独计算 = 期内分红 × 剩余份额
3. 总收益 = 浮动盈亏 + 分红收益 + 退出利得 - 业绩报酬
