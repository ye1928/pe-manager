# 2026-05-10 工作记录

## 周期收益Tab基金过滤问题修复

**问题**：周期收益Tab只显示7只基金，但用户持有13只基金。

**原因**：`renderPeriodAnalysis()` 使用 `getNavFitFunds()` 过滤基金，只保留 ≥5条净值数据的基金。

**修复**：修改 `renderPeriodAnalysis()`：
- 改用 `allFunds = funds.filter(f => (f.batches || []).some(b => !b.exitDate))` 获取所有持有基金
- 用 `fundsWithNav` 获取有净值数据的基金，用于计算周期列表
- 无数据的年度显示 `--`，不再过滤掉基金

**修改文件**：`c:/Users/52706/WorkBuddy/20260425195840/investment-manager.html`
